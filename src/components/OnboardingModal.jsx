// OnboardingModal is no longer used — the onboarding flow was removed.
// Registration number is now just an optional profile attribute set in ProfileModal.
// This file is kept as a no-op placeholder so any stale imports don't crash the build.
export default function OnboardingModal() {
    return null;
}
